// jQuery starts with $(), which is a stand in for jQuery().

// $( handler) is the recommended way to code jQuery. It stands for
// $(document).ready( handler ), which executes the code in the handler when
// the webpage DOM is ready.
$(function () {
  $()
})
